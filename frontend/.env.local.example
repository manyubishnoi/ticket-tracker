# Backend API base URL.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Shared token used to sign requests to the notification service.
# Exposed to the browser because the client calls the provider directly.
NEXT_PUBLIC_NOTIFY_API_KEY=notify_live_8fb2c1a9d4
